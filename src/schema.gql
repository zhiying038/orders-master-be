# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ItemImage {
  id: String!
  link: String!
  alt: String
  item: Item!
}

type Item {
  id: String!
  code: String!
  name: String!
  price: Float!
  currency: String!
  images: [ItemImage!]
}

type Order {
  id: String!
  createdAt: DateTime!
  placedAt: DateTime!
  referenceNumber: String!
  totalPrice: Float!
  currency: String!
  orderDetails: [OrderDetail!]
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Orders {
  items: [Order!]!
  total: Int!
  hasMore: Boolean!
  page: Int!
  pages: Int!
}

type OrderDetail {
  quantity: Int!
  id: String!
  unitPrice: Float!
  item: Item!
  order: Order!
}

type Price {
  currency: String!
  price: Float!
}

type SignedUrl {
  signedUrl: String
}

type Query {
  getItems(searchText: String): [Item!]!
  getPaginatedOrders(options: CommonFilterOptionInput!, filter: FilterOrderInput): Orders!
  getOrderById(id: Int!): Order!
  calculateTotalPrice(input: PlaceOrderInput!): Price!
  getNextReferenceNumber: String!
}

input CommonFilterOptionInput {
  page: Int = 1
  limit: Int = 10
  sortKey: String
  sortOrder: SortOrder = ASC
}

enum SortOrder {
  ASC
  DESC
}

input FilterOrderInput {
  referenceNumber: String
  placedAt: DateTime
}

input PlaceOrderInput {
  placedAt: DateTime
  orders: [CreateOrderDetailInput!]!
}

input CreateOrderDetailInput {
  quantity: Int!
  unitPrice: Float!
  itemCode: String!
  orderId: Float
}

type Mutation {
  createItem(input: CreateItemInput!): Item!
  updateItem(input: UpdateItemInput!): Item!
  deleteItems(codes: [String!]!): Boolean!
  createOrder(input: PlaceOrderInput!): Order!
  generateSignedUrl(input: CreateUploadInput!): SignedUrl!
}

input CreateItemInput {
  code: String!
  name: String!
  price: Float!
  currency: String
  images: [AddItemImageInput!]
}

input AddItemImageInput {
  link: String!
  alt: String
}

input UpdateItemInput {
  code: String!
  name: String
  price: Float
  currency: String
  images: [AddItemImageInput!]
}

input CreateUploadInput {
  purpose: UploadPurpose!
  mimeType: String!
}

enum UploadPurpose {
  IMAGE
}